node_modules/
dist/
.cache/
.venv/
.DS_Store

# Claude Code worktrees: full checkouts that live inside the repo
.claude/worktree/

# Apple's iPhone Duo model, fetched per clone: python3 scripts/prepare-model.py
# No trailing slash: a worktree usually links this at the main checkout's copy.
public/model

# Blender scratch output
design/blender/tex/
design/blender/render/
design/blender/*.blend1

# Tauri regenerates these on every build
packages/shell/desktop/gen/

# The Duo films (HyperFrames) and their app captures stay on the machine that made them
/video/
