# UI kit

Existing UI, tokens, icons and shared helpers extracted without redesign. The React `App` adapter consumes SDK host types. Sample tracks are shared fixture data, not a playback service. Assets are served from root `public/icons/`. Component harvest and standalone publishable assets remain future work.
