# SDK

Private scaffold. The existing baked-app host types live in `legacy.ts`; they are not a sandbox protocol or a stable public API. Manifest, bridge, display and lifecycle contracts remain future work.
